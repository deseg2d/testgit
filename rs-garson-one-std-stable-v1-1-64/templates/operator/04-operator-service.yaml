apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.operator.service.name }}
  name: {{ .Values.operator.service.name }}
spec:
  type: {{ .Values.operator.service.type }}
  ports:
    - name: "http"
      port: {{ .Values.operator.service.port }}
      targetPort: {{ .Values.operator.service.targetPort }}
  selector:
    app: {{ .Values.operator.service.name }}
