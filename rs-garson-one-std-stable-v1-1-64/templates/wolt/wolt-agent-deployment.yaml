{{- if .Values.woltAgent.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.woltAgent.service.name }}
  name: {{ .Values.woltAgent.service.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.woltAgent.service.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.woltAgent.service.name }}
    spec:
      containers:
        - name: {{ .Values.woltAgent.service.name }}
          image: "{{ .Values.woltAgent.image.repository }}:{{ .Values.woltAgent.image.tag }}"
          ports:
            - containerPort: {{ .Values.woltAgent.service.targetPort }}
              protocol: TCP
          env:
          {{- range .Values.woltAgent.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
          {{- end }}
      restartPolicy: Always
{{- end}}