apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.kioskSimulator.service.name }}
  name: {{ .Values.kioskSimulator.service.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.kioskSimulator.service.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.kioskSimulator.service.name }}
    spec:
      containers:
        - name: {{ .Values.kioskSimulator.service.name }}
          image: "{{ .Values.kioskSimulator.image.repository }}:{{ .Values.kioskSimulator.image.tag }}"
          ports:
            - containerPort: {{ .Values.kioskSimulator.service.targetPort }}
              protocol: TCP
          env:
          {{- range .Values.kioskSimulator.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
          {{- end }}
      restartPolicy: Always
